{
  "name": "db",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "docker compose up --build",
    "db:push": "cross-env DATABASE_URL=postgresql://postgres:postgres@localhost:5432/worship_confluence npx drizzle-kit push",
    "db:generate": "npx drizzle-kit generate",
    "db:studio": "npx drizzle-kit studio",
    "db:seed": "tsx src/seedDb.ts"
  },
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.20.0",
    "cross-env": "^10.1.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.21.0"
  }
}
